#include <stdio.h>
#include <stdlib.h>

/**
 * main - A programme that prints single digits num of base 10 from 0
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");

	return (0);
}
